<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Winveer" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/winveer_background</item>
        <item name="colorPrimaryVariant">@color/winveer_background</item>
        <item name="colorOnPrimary">@color/winveer_background</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/winveer_background</item>
        <item name="colorSecondaryVariant">@color/winveer_background</item>
        <item name="colorOnSecondary">@color/winveer_background</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>